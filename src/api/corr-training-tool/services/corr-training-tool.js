'use strict';

/**
 * corr-training-tool service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::corr-training-tool.corr-training-tool');
