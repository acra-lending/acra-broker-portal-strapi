'use strict';

/**
 * corr-training-tool router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::corr-training-tool.corr-training-tool');
