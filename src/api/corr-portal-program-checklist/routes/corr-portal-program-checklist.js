'use strict';

/**
 * corr-portal-program-checklist router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::corr-portal-program-checklist.corr-portal-program-checklist');
