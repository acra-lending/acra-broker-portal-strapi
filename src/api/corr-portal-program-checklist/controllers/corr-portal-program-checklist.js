'use strict';

/**
 * corr-portal-program-checklist controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::corr-portal-program-checklist.corr-portal-program-checklist');
