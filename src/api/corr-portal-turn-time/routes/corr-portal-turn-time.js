'use strict';

/**
 * corr-portal-turn-time router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::corr-portal-turn-time.corr-portal-turn-time');
