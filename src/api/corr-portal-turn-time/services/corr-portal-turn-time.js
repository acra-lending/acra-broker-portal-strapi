'use strict';

/**
 * corr-portal-turn-time service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::corr-portal-turn-time.corr-portal-turn-time');
